{
  "name": "music-journal-backend",
  "version": "1.0.0",
  "description": "Express and MongoDB service for Music Journal",
  "private": true,
  "homepage": "https://github.com/vldstassh/music-journal#readme",
  "bugs": {
    "url": "https://github.com/vldstassh/music-journal/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vldstassh/music-journal.git"
  },
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check server.js && node --check connection/connection.js && node --check controllers/authController.js && node --check controllers/moodController.js && node --check lib/validation.js && node --check middleware/auth.js && node --check middleware/passwordHash.js && node --check models/authModel.js && node --check models/moodModel.js && node --check routes/authRouter.js && node --check routes/moodRoutes.js && node --check ../public/config.js && node --check ../public/script.js && node --check ../public/login.js",
    "test": "npm run check && node --test",
    "test:watch": "node --test --watch",
    "dev": "nodemon server.js"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "connect-mongo": "^6.0.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-session": "^1.19.0",
    "mongodb": "^7.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
